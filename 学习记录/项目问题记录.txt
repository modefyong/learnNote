1.UUIDUtil.getUUID()什么意思？为什么不用自增数字序号？
2.(@RequestBody MenuUpdateRequest menu){//传递了数组，前台放在payload里面了，后台通过@RequestBody获取
payload是什么？payload(有效载荷)就是对于接收者有用的数据

3.遇到一个问题：
做一个积分表，需要关联用户表，在页面上显示全部用户的积分。当我增加积分记录的时候，怎么知道我增加的谁的积分？如果是新用户呢，之前积分表里没有新用户的积分纪录，怎么新增积分纪录和新用户关联起来？因为要统计每个用户的总分，所有积分表里需要新增一个总分字段吗？自己手动计算？？？不能手动计算。那怎么办？


数据库问题：
如何求数据库表某一行的和？
select (字段1+字段2+。。。字段n) as all_sec from 表名 where id=?
不能用插入语句，要用修改sql语句
update inter_intergral_show set all_sc = '220' where id=1

积分列表
4.积分列表要增加字段怎么办？修改表结构，将字段名改变成数据。

5.数据库设计的三大范式如下：
第一范式 每一个分类必须是一个不可分的数据项。属性不可再分，确保每列的原子性。
第二范式 要求每个表只描述一件事情，每条记录有唯一标识列。
第三范式 数据库表中不包含已在其它表中已包含的非主关键字信息。

6.难点：layui动态生成表头。网上照着改。
我想在前台获得数据库积分表里的类别和分数对应的用户。
问题1：不知道怎么获得用户id（参数）。后台根据用户id获取积分列表
问题2：积分表怎么只显示每个人的单独信息？不会出现多条用户信息。getAll时sql。分组。前台拼接成指定数组对象。
问题3：layui中 field必须和数据库字段名对应。使用拼接的已知数据赋值。
js难点：去重，数组对象按指定key合并为一个对象。
7.做项目功能模块，先做依赖少的模块，便于联系改动。例如：积分系统的积分列表和积分加分详情，应该先做积分加分详情。
8.完成任务：角色权限分配，菜单管理。
1.每个用户只能查看个人积分。
2.每个用户只能查看自己添加的投诉建议。
9.难点：添加文件。
10.默认显示某个种类的信息。怎么做？layui数据表格自带where属性。
11.我把js和java混淆了。
12.主管权限分不用添加到积分列表详情表，直接添加到积分列表即可。
13.检查service层方法从数据库获得的信息是否为空。空指针异常要及时做处理！
14.在企业中数据库表里的数据一般不会真正删除，只需要加一个是否删除的状态字段即可。
15.parseInt()将字符串转成相应的整数。浮点数以后的数字都不要了。
使用parseFloat()方法的另一不同之处在于，字符串必须以十进制形式表示浮点数，parseFloat()没有基模式。
方法toFixed，含有四舍五入的运算
Math.ceil();    //向上取整
Math.round();   //四舍五入取整。
Math.floor();   //向下取整
16.控制层更新操作必须传入id参数，不能直接传入实体对象作为形参。
17.后台参数位置要对应。
18.严重！严格控制访问ajax次数！
19.sql语句不会写，统计用电监管平台不同状态的设备数量。
20.只关注表面功能，忽视了内部原理具体实现，总是有种模棱两可的感觉。
21.mysql 的sql语句编写不熟练，能在后台处理，或数据库持久层处理的数据不要放到前台去处理，数据量大时容易导致访问数据过慢。
22.//      监听时间日期选择器
laydate.render({
            elem: '#timeDate',
            done: function(value, date) {
                eleHours = [];

                // 在这个方法里处理eleHours
                initElecGraph(value);


                //这里一直为空！获取不到eleHours，原因：我重复声明了相同名称的变量，具体深层原因：js内部机制，变量提升。
                console.log(eleHours);

                "···········"

                var eleHours = ["··········"];
            }
        });

23.js判断一个数组是否为空，不能直接写if(arr = [])，严格区分 “=”和 “==”的区别与应用。也不能用if(arr== [])判断，应该判断数组长度 if(arr.length == 0)。
原因：我认为 if(arr = [])中的 [] 其实是 字符串“[]”，而不是数组 []。

24.写代码注意事项（经验）：
1.变量起名不要用 xx1，xx2 ····的形式；
2.重复代码，及时封装组件；
3.仔细查看代码用处，删除无用的代码。
4.注释要简单明确；
5.统计数据尽量在后台处理，减少前台访问ajax次数。

25.数据库表新增加字段时，mybatis，xml文件中一定要添加上，否则会报错。
26.反思总结：
做不出来的原因：
1.对java中的map语法比较陌生，不能熟练使用；
2.拼接数据时应该先考虑新增数据，再对某字段进行判断，否则会混乱。
3.知识盲区：Map.Entry<> 和aa.entrySet()。
4.对于编写这种业务需求的代码，经验匮乏。

解决办法：
1.了解map基本语法，扩展到了解java所有基本语法的基本业务场景。
2.多动手实践！缺乏项目经验。
 for (Map.Entry<String, PointSituationRsp> m : industryMap.entrySet()) {
            //System.out.println("key:" + m.getKey() + " value:" + m.getValue());
          standardData.add(m.getValue());
        }
博客地址：https://blog.csdn.net/q5706503/article/details/85122343?depth_1-utm_source=distribute.pc_relevant.none-task&utm_source=distribute.pc_relevant.none-task

27.service层判断传入的参数是否为空，目的是为了减少无效的数据库访问。
28.沟通问题：交付任务时一定要确认对方收到你的消息。这是你的缺点。
29.后台接口注意空格，有时候传参时会出传不到后台。
