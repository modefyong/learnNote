service层提供一个接口
实现：返回值
目的：返回一个实体类对象。
@Override
public HospitalDepartment queryHospitalDepartmentById(Long id) {
    HospitalDepartment hospitalDepartment = (HospitalDepartment) 	    hospitalDepartmentDao.queryOne("selectByPrimaryKey",id);
            return hospitalDepartment;
}
1.获取action？之后的参数
String userId = this.getParameter("updateUserId");
2.
/~HOSPITAL_NAME: and HOSPITAL_NAME like '%'||{HOSPITAL_NAME}||'%'~/
这句代码的意思是当页面有查询组件的时候才会执行。查询条件参数。

3.各个service里面只能调用自己的Dao。

4.DB2 关联表更新
update TBL_USER set STATUS= 'ENABLED' from TBL_USER_ROLE where TBL_USER.ID = TBL_USER_ROLE.USER_ID and TBL_USER_ROLE.ROLE_ID = -3 and TBL_USER.ORGANIZATION_ID = #{value,javaType=long}

5.nexus 登录：http://192.168.2.105:53214

6.dao层调用多个方法必须加事务注解

7.调用Dao，sql语句时，两个参数以上必须写测试类

8.医疗机构、处置单位、运送单位共用一张用户（user）表，所以每次禁用自己单位用户时一定要先判断是否属于自己单位！！！

9.spring 查询组件参数，区分like和 = 名称用like，ID，类型必须用=。

10.页面报异常，查看路径：检查网页链接是否正确。

11.spring配置文件，数据库查询sql：where 1=1是为了方便后面参数加and
下拉选中的name是为了向后台传参，是getParameter里的key。
12.
bug出现原因：
1.数据库 insert 语句修改名字后，menu权限不对应
2.身份卡两个Id 报错：Attribute qualified names must be unique within an element
3.处置中心看到别的系统的账户信息 bug原因：处置单位查询组件参数Id，即为组织结构ID。

13.debug
快捷键
F8表示当前实现继续运行直到下一个断点。

F5表示进入当前方法。

F6表示运行下一行代码。

F7表示退出当前方法，返回到调用层。

14.下拉列表：
问题1：怎么从数据库中获取负责人姓名（用户）？具体在那个文件写？看源码。
从哪个表里取数据就在对应表的Dao层创建对应的方法
//根据组织结构Id 获取用户名
public List<User> queryUserByOrganizationId(Long organizationId);
2.从Dao层传到service层，service层方法：
public List<UserDto> getUserByOrganizationId(Long organizationId);
3.跳转科室的时候调用service层方法
将集合存放到Request里
this.setRequestAttribute("userDtoList", userDtoList);
4.jsp问题。
struts2的select标签
listKey="id" listValue="realName"

5.空指针错误，注释错了对应的action。寻找文件----

6.跳转路径不在一个namespace下，action要使用相对路径
<a href='../waste/showMedicalWasteTraceProcedure.action?medicalWasteId=${ID}'>${TRACE_CODE}</a>

7.修改bug时，不知道该句代码什么用就不要随便删除代码，会引起其他bug！严重。
8.<s:hidden name="userDto.id" key="userDto.id"/>
						<s:hidden name="userDto.realName" key="userDto.realName"/>
						<s:hidden name="userDto.name" key="userDto.name"/>
struts2 隐藏域标签，用key代替value。